export const fetchShoppingLists = async () => {
  // Fetch shopping lists from the server
};

export const fetchShoppingList = async (id) => {
  // Fetch a single shopping list by ID from the server
};

export const createShoppingList = async (data) => {
  // Create a new shopping list on the server
};

export const addItem = async (data) => {
  // Add an item to a shopping list on the server
};

export const editItem = async (id) => {
  // Edit an item in a shopping list on the server
};

export const deleteItem = async (id) => {
  // Delete an item from a shopping list on the server
};

export const inviteToShoppingList = async (data) => {
  // Invite a user to a shopping list on the server
};